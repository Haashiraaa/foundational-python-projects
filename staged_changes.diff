diff --git a/projects/number-analyzer/src/number_analyzer/input_validator.py b/projects/number-analyzer/src/number_analyzer/input_validator.py
index 6b1c3f3..09b29a4 100644
--- a/projects/number-analyzer/src/number_analyzer/input_validator.py
+++ b/projects/number-analyzer/src/number_analyzer/input_validator.py
@@ -1,25 +1,22 @@
-
-
 # input_validator.py
 
 
 import logging
+import sys
 from haashi_pkg.utility import Logger, ScreenUtil as su
 from typing import Optional
-from number_analyzer.types import NumberLike, SquareRoot
+from number_analyzer.types import NumberLike
 from number_analyzer.number_utils import NumberUtils
 
 
 class InputHandler:
-
     @staticmethod
     def get_input(
         msg0: Optional[str] = None,
-        msg1: Optional[str] = "or press 'q' to quit.",
+        msg1: Optional[str] = "or press 'q' to return back to main menu.",
         prompt: str = ">>> ",
-        logger: Optional[Logger] = None
+        logger: Optional[Logger] = None,
     ) -> Optional[NumberLike]:
-
         logger = logger or Logger(level=logging.INFO)
         su.space()
         if msg0:
@@ -31,14 +28,12 @@ class InputHandler:
 
     @staticmethod
     def parse_input(
-        user_input: str,
-        logger: Optional[Logger] = None
+        user_input: str, logger: Optional[Logger] = None
     ) -> Optional[NumberLike]:
-
         logger = logger or Logger(level=logging.INFO)
 
-        if user_input == 'q':
-            return
+        if user_input == "q":
+            return None
 
         try:
             if isinstance(user_input, int):
@@ -47,59 +42,60 @@ class InputHandler:
             return float(user_input)
 
         except ValueError:
-            logger.warning(
-                f"{user_input} is not a valid integer or float."
-            )
+            logger.warning(f"{user_input} is not a valid integer or float!")
             logger.info("Falling back to main menu...")
             return None
 
 
 class NumberController:
-
     @staticmethod
-    def check_even(logger: Optional[Logger] = None) -> Optional[bool]:
-
+    def check_even(logger: Optional[Logger] = None) -> Optional[str]:
         logger = logger or Logger(level=logging.INFO)
 
         num = InputHandler.get_input(
-            msg0="Enter a number to check if it's even:",
-            prompt=">>> ",
-            logger=logger
+            msg0="Enter a number to check if it's even", prompt=">>> ", logger=logger
         )
-        if isinstance(num, int):
-            if NumberUtils.is_even(num):
-                return True
-            return False
-        return None
 
-    @staticmethod
-    def check_prime(logger: Optional[Logger] = None) -> Optional[bool]:
+        if num is None:
+            return num
 
+        if NumberUtils.is_even(num):
+            return f"{num} is even."
+        return f"{num} is not even."
+
+    @staticmethod
+    def check_prime(logger: Optional[Logger] = None) -> Optional[str]:
         logger = logger or Logger(level=logging.INFO)
 
         num = InputHandler.get_input(
-            msg0="Enter a number to check if it's prime:",
-            prompt=">>> ",
-            logger=logger
+            msg0="Enter a number to check if it's prime", prompt=">>> ", logger=logger
         )
-        if isinstance(num, int):
-            if NumberUtils.is_prime(num):
-                return True
-            return False
-        return None
 
-    @staticmethod
-    def check_square_root(
-        logger: Optional[Logger] = None
-    ) -> Optional[SquareRoot]:
+        if num is None:
+            return num
+
+        if NumberUtils.is_prime(num):
+            return f"{num} is prime."
+        return f"{num} is not prime."
 
+    @staticmethod
+    def check_square_root(logger: Optional[Logger] = None) -> Optional[str]:
         logger = logger or Logger(level=logging.INFO)
 
         num = InputHandler.get_input(
-            msg0="Enter a number to calculate its square root:",
+            msg0="Enter a number to calculate its square root",
             prompt=">>> ",
-            logger=logger
+            logger=logger,
         )
-        if isinstance(num, int) or isinstance(num, float):
-            return NumberUtils.square_root(num)
-        return None
+
+        if num is None:
+            return num
+
+        result, rounded_to, rounded, sf = NumberUtils.square_root(num)
+        formatted_result = (
+            f"Square root check for {num} "
+            f"Exact value: {result} | Rounded ({sf} sf): {rounded_to} | "
+            f"Integer: {rounded}"
+        )
+
+        return su.format_text(formatted_result)
diff --git a/projects/number-analyzer/src/number_analyzer/main.py b/projects/number-analyzer/src/number_analyzer/main.py
index b1a0485..972b8e9 100644
--- a/projects/number-analyzer/src/number_analyzer/main.py
+++ b/projects/number-analyzer/src/number_analyzer/main.py
@@ -1,4 +1,3 @@
-
 # main.py
 
 
@@ -9,19 +8,28 @@ from number_analyzer.input_validator import InputHandler, NumberController
 from haashi_pkg.utility import Logger, ScreenUtil as su
 
 
+choice_map = {
+    1: NumberController.check_even,
+    2: NumberController.check_prime,
+    3: NumberController.check_square_root,
+}
+
+
 # ======================
 # Main Menu
 # ======================
 
-def main_menu() -> None:
+
+def main_menu(logger: Optional[Logger] = None) -> None:
     """Displays the list of available operations.
     Helps users choose what action to perform.
     """
+    logger = logger or Logger(level=logging.INFO)
     options: list[str] = [
         "Even or Odd Checker",
         "Prime Checker",
         "Square Root Calculator",
-        "Exit."
+        "Exit.",
     ]
 
     logger.info("Choose an operation:")
@@ -33,53 +41,57 @@ def main_menu() -> None:
 # Main
 # ======================
 
-def main(logger: Optional[Logger] = None) -> None:
 
+def main(logger: Optional[Logger] = None) -> None:
     logger = logger or Logger(level=logging.INFO)
 
     su.space()
     logger.info("Hello! Welcome to the Number Analyzer!")
-    logger.info(
-        "This program will help you with some mathematical operations."
-    )
+    logger.info("This program will help you with some mathematical operations.")
     su.space()
+    su.wait_and_enter()
 
     while True:
-
+        su.clear_screen()
         main_menu()
 
         try:
             user_choice = InputHandler.get_input(
-                prompt="Enter your choice (1-4):",
-                logger=logger
+                prompt="Enter your choice (1-4): ", logger=logger
             )
 
-            if user_choice == 1:
-                validate.validate_even_odd()
+            if user_choice == 4:
+                logger.info("Exiting Program...")
+                sys.exit(0)
 
-            elif user_choice == 2:
-                validate.validate_prime()
+            if user_choice and user_choice > 4:
+                logger.info(f"Input: ({user_choice}) is out of range!")
+                continue
 
-            elif user_choice == 3:
-                validate.validate_square_root()
+            if user_choice is None:
+                continue
 
-            elif user_choice == 4:
-                logger.info("\nExiting...")
-                ut.clear_line(n=3, timeout=1)
-                sys.exit(0)
+            func = choice_map.get(int(user_choice))
+            if not func:
+                logger.warning(f"Input: ({user_choice}) is invalid")
+                continue
+
+            result = func()
+            if result is None:
+                continue
+            logger.info(result)
+            su.wait_and_enter()
 
-            else:
-                logger.info("\nChoice out of range!")
-                ut.clear_line(n=3, timeout=0.2)
+        except KeyboardInterrupt:
+            su.space()
+            logger.info("Program interrupted by user.")
+            sys.exit(0)
 
-        except ValueError:
-            logger.info("\nInvalid choice!")
-            ut.clear_line(n=3, timeout=0.2)
+        except Exception as exc:
+            logger.error(f"An error occurred: {exc}")
+            logger.error(exception=exc, save_to_json=True)
+            sys.exit(1)
 
 
 if __name__ == "__main__":
-    try:
-        main()
-    except KeyboardInterrupt:
-        logger.info()
-        sys.exit(0)
+    main()
